name: test bats

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    
    steps:
      - uses: actions/checkout@v2
      - name: install bats
        run: sudo apt-get install -y bats
        
      - name: bats test
        run: bats ex3/test.sh
